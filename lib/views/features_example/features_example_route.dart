import 'package:flutter/material.dart';

class FeaturesExampleRoute extends StatelessWidget {
  const FeaturesExampleRoute({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}